{eval $page_rel='goods_over';}
<script type="text/javascript">
function ajaxTodo_groupon_re_selling(json) {
    DWZ.ajaxDone(json);
    if(json.statusCode == 200) {
        // 成功
        // 刷新。提交一次
        $("form", $("#groupon_{$page_rel}")).submit();
    }
}
</script>
<div class="pageHeader" id="groupon_{$page_rel}">
    <form id="pagerForm" method="post" action="{$current_url}" onsubmit="return divSearch(this, '{$page_rel}');">
        <input type="hidden" name="pageNum" value="{$cur_page}"/>
        <input type="hidden" name="numPerPage" value="{$per_page_num}"/>
        <input type="hidden" name="orderField" value="{$order_field}"/>
        <input type="hidden" name="orderDirection" value="{$order_direction}"/>
        <div class="searchBar">
            <table class="searchContent">
                <tr>
                    <td>
                    <select name="source_status">
                        <option value="0">对方团购状态</option>
                        <option value="1"<!--{if $source_status=='1'}--> selected<!--{/if}-->>下架</option>
                        <option value="2"<!--{if $source_status=='2'}--> selected<!--{/if}-->>上架</option>
                    </select>
                    <input type="text" name="keywords" value="{$keywords}" />
                    <button type="submit">
                        检索
                    </button></td>
                </tr>
            </table>
        </div>
    </form>
</div>
<div class="pageContent">
    <div class="panelBar">
        <ul class="toolBar">
            <li>
                <a class="add" target="navTab" rel="{eval echo build_rel(array('my_goods', 'groupon_view'))}" href="{eval echo site_url(array('my_goods', 'groupon_view', 'id'))}/<!--{{id}}-->" warn="请选择您要查看的团购"><span>查看团购</span></a>
            </li>
            <li>
                <a class="add" callback="ajaxTodo_groupon_re_selling" target="ajaxTodo" rel="{eval echo build_rel(array('my_goods', 'groupon_re_selling'))}" href="{eval echo site_url(array('my_goods', 'groupon_re_selling', 'id'))}/<!--{{id}}-->" title="您确定要再次上架该团购么？确定好哦？亲。" warn="请选择您再次上架的团购"><span>再次上架</span></a>
            </li>
            <li>
                <a class="icon" target="myDwzExport" title="您确定要导出团购订单么？" href="{eval echo site_url(array('my_goods', 'export_order', 'type', '12', 'id'))}/<!--{{id}}-->" warn="请选择要导出订单信息的商品"><span>导出订单明细</span></a>
            </li>
        </ul>
    </div>
    <table class="table" width="100%" layoutH="158" rel="{$page_rel}">
        <thead>
            <tr>
                <th width="5%">商品ID</th>
                <th width="40%">标题</th>
                <th width="15%">开始时间</th>
                <th width="15%">结束时间</th>
                <th width="5%">价格</th>
                <th width="5%">参与人数</th>
                <th width="5%">我方(订单/份数)</th>
                <th width="5%">来源</th>
                <th width="5%">对方状态</th>
            </tr>
        </thead>
        <tbody>
            <!--{loop $list $row}-->
            <tr target="id" rel="{$row['id']}" id="group_selling_{$row['id']}">
                <td>{$row['id']}</td>
                <td><a href="{$row['teamUrl']}" target="_blank">{$row['title']}</a></td>
                <td>{$row['startDate']}</td>
                <td>{$row['endDate']}</td>
                <td>{$row['grouponPrice']}</td>
                <td>{$row['joinedCount']}</td>
                <td>{$row['orderNum']}/{$row['saleNum']}</td>
                <td>{$row['sourceName']}</td>
                <td>{eval echo $row['sourceStatus']?'上架':'下架'}</td>
            </tr>
            <!--{/loop}-->
        </tbody>
    </table>
    <!--{template main/page_bar}-->
</div>
<script type="text/javascript">
    $(function(){
        if ($.fn.myDwzExport) {
            $("a[target=myDwzExport]", $(document)).myDwzExport();
        }
    });
</script>

